<h1 class="page-header">Edit</h1>

<%= simple_form_for ([@course, @slide]) do |f| %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "select a chapter")%>
  <%= f.input :image, as: :file %>
  <%= f.button :submit %>
<% end %>