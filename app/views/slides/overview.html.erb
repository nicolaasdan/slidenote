<h1>Overview</h1>

<% @slides.each do |slide| %>
  <div class="col-md-3">
    <%= link_to image_tag(slide.image, class: "overview-slides"), course_slides_path(slide.course, slide) %>
  </div>
<% end %>