 <h1 class="page-header"><span class="glyphicon glyphicon-stats"></span> Leaderboard</h1>

<h3>Top 5 Most valuable users</h3>

<table class="table table-striped" idd="css-serial">
  <thead>
    <tr>
      <th>#</th>
      <th>First Name</th>
      <!--<th>Last name</th>-->
      <th>Level</th>
      <th>Score</th>
      <th>Latest note</th>
    </tr>
  </thead>
  <tbody>
    <% @users.order(:score => :desc).limit(5).each do |user| %>
      <tr>

        <td></td>
        <td id="link"><%= link_to user.first_name, user_path(user) %></td>
        <!--<td><%= user.last_name %></td>-->        
        <td><span class="<%= user.check_level %>"><%= user.level %></span></td>
        <td><strong><%= user.score %></strong></td>
        <td>
          <% unless user.notes.last.nil? %>
            <%= time_ago_in_words(user.notes.last.created_at) %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<!--
<h3>Best comments</h3>

<table class="table table-striped" idd="css-serial">
  <thead>
    <tr>
      <th>#</th>
      <th>First Name</th>
      <th>Last name</th>
      <th>Comment</th>
      <th>Total Score</th>
    </tr>
  </thead>
  <tbody>
    <% @notes.order("cached_votes_score DESC").limit(5).each do |note| %>
      <tr>

        <td></td>
        <td><%= note.user.first_name %></td>
        <td><%= note.user.last_name %></td>
        <td><%= note.comment %></td>
        <td><%= note.cached_votes_score %></td>      
      </tr>
    <% end %>
  </tbody>
</table>
-->